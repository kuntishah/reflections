What happens when you initialize a repository? Why do you need to do it?

git folder is made and it tracks history for file

How is the staging area different from the working directory and the repository? What value do you think it offers?

working directory is not on it. repository is part of git itself and contains history of all the files.
staging area lets us make logical commits. I can add files to staging area and commit. Then add more files and commit logically.

How can you use the staging area to make sure you have one commit per logical change?

add files to stagin area that cater to one logical change only. And commit those changes with an approproiate
commit message. Doing this helps track all the logical changes made.

What are some situations when branches would be helpful in keeping your history organized? How would branches help?

branches do not interfere with the main logic. Different logic to the same problem can
be solved under different branches and can be checked if they work right. 

How do the diagrams help you visualize the branch structure?

they are more clearer and explain concept well

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?

Two codes working on sa,e project but different featears are merged together. We represent in the diagram that coins merges in to master.
because coins does merge into master which is the main code.


What are the pros and cons of Git’s automatic merging vs. always doing merges manually?

Git does it for you and it is a little easier
